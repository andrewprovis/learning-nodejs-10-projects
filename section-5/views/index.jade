extends layout

block content
  if posts
    each post, i in posts
      .post
        h1
          a(href='/posts/show/#{post._id}')
            =post.title
        p.meta Posted in #{post.category} by #{post.author} on #{post.date}
        =post.body
        a.more(href='/posts/show/#{post._id}') Read more
  else
    p.noposts There are no posts to display